const a = 10
const b = 20
const result = 'The sum of number '+ a + " and "+ b + " is"+ " "+ (a+b)
const result2 = `The sum of number ${a} and ${b} is ${a + b}`
console.log(result);
console.log(result2);

// ``That is es6 and have a supper power if you use tamplate string
const challenge = `hello world how 
is it going`
console.log(challenge);